Aim:Write a program using Kotlin to implement delegation and extension function.

interface delegation
{
    fun mymessage()
    fun mymessageline()
}

class delegationimplementation(val y: String) : delegation
{
    override fun mymessage()
    {
        print(y)
    }
    override fun mymessageline()
    {
        println(y)
    }
}

class Newfeature(m: delegation) : delegation by m
{
    override fun mymessage()
    {
        print("Android with Kotlin Programming")
    }
}

// Main function
fun main()
{
    val b = delegationimplementation("\nWelcome, SYCS!")

    Newfeature(b).mymessage()
    Newfeature(b).mymessageline()


}


fun operation(a: Int, b: Int, operation: (Int, Int) -> Int): Int {
        return operation(a, b)
    }

    fun sum(a: Int, b: Int): Int = a + b
    fun multiply(a: Int, b: Int): Int = a * b

    fun main() {
        val addFunction: (Int, Int) -> Int = ::sum
        val multiplyFunction: (Int, Int) -> Int = ::multiply
print("Addition of nos is:")
        println(operation(10, 5,addFunction))
        print("Multiply nos is:")
        println(operation(10, 5, multiplyFunction))
    }





//EXTENSIVE FUNCTION
class Circle(val radius: Double) {
    fun area(): Double {
        return Math.PI * radius * radius
    }
}

// Extension function
fun Circle.perimeter(): Double {
    return 2 * Math.PI * radius
}

fun main() {
    val circle = Circle(2.5)
    println("Area of the circle is ${circle.area()}")
    println("Perimeter of the circle is ${circle.perimeter()}")
}
